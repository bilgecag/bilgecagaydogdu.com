.DS_Store

# Local-only — kept on disk, never pushed to GitHub.
# Git holds just the site (public/) plus its deploy config (wrangler.jsonc).
.claude/
CLAUDE.md
material/

# Camera originals — multi-MB, not needed to serve the site.
IMG_*.jpg
portrait.jpg
portrait2.jpg

# Unrelated document that happens to live in this folder
Bilgecag Aydogdu  3276825WR LVL4-6ALL+EREENTERPRISE.004.pdf

# wrangler local build cache
.wrangler/
